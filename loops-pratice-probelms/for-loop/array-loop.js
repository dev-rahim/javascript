// target display all element of an array using for loop

const numbers = [45, 85, 65, 96, 36, 26, 52, 63];

for (let i = 0; i < numbers.length; i++) {
  const element = numbers[i];
  //   console.log(element);
}

// target display all element of an array using while loop

let i = 0;
while (i < numbers.length) {
  console.log(numbers[i]);
  i++;
}
